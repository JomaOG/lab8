
/**
 * Write a description of class Answers here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class Answers
{
    // Question 14:
        // setUp()
        // tearDown()
    
    // Question 17:
        // test failed because the expected return from the comment
        // with a rating of 0 should be false, but it returns true. This
        // is because the ratingInvalid method return true
        // if rating is < 0. The correction is rating < 1
        
    // Question 22:
        // No, since it just a printed message.
        
    // Question 23:
        // The addition operation should return 7 instead it returns 1
        
    // Question 24:
        // The substration operation should return 5 instead it return -13
        
    // Question 25:
        // Probably error in the code in the CalcEngine.class;
        
    // Question 26:
        // clear();
        // numberPressed(3);
        // plus();
        // numberPressed(4);
        // equals();
        // getDisplayValue();
        
    // Question 27:
        // equals(), applyPreviousOperator(), and previousOperator() are fine
        
    // Question 28:
        // clear() need to set previousOperator to null; and set leftOperand to null
        // getDisplayValue() is fine;
        
    // Question 29:
        // changing the numberPressed(int number) method so that the body is just
        // displayValue = displayValue + number
        
    // Question 30:
        // I created a unit test that performs the walkthrough of question 30
}
